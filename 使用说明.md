# 🐉 龙区分类器

基于断箭官方 GameLogs 日志和 [BATrace](https://app.batrace.top/) 公开数据的本地对局复盘工具。
Fork 自 Zola 的 [断箭蛆工具](https://github.com/Zawinzala/brokenarrow-log-maggot)，和原版的区别：

- **去掉了所有连接原作者服务器的功能**：心跳统计 / 在线人数、房间内工具用户、启动公告与版本推送、新版本下载。现在只访问 BATrace 公开接口（运营方已同意本工具使用其 API），以及 GitHub 上本仓库的 Release 信息（新版本提醒）。
- **龙区分取代了蛆指数**，新增单局龙区复盘和称号。
- **重写了对局录像**：FFmpeg 采集 + 显卡硬件编码，存 MP4，支持 HDR 显示器，能录系统声音，进度条可随意拖动。
- 界面只保留中文；去掉了 API 调用额度、APM 统计、开发者测试。

## 首页

从上到下：**当前对局** → **玩家查询 · 龙区分** → **对局档案 / 行车记录仪** → **卡组工具**（默认折叠，点标题栏展开）→ **封禁追踪**（默认隐藏）。顺序可以在「设置 → 面板次序」里调。

## 功能

- **日志实时监听**：监听你设置的 GameLogs 目录，自动识别当前对局（地图 / 对局 ID / 玩家名单 / 队伍 / 卡组）。除原生 `Gamelog__*.log` 外也兼容自放的 `.log/.txt` 文本日志（多个文件时原生日志优先）。
- **房间内玩家**：开战前就能看到房内玩家名单（日志 Incoming client 行，ID 即 BATrace ID），点「刷新粗查」提前看情报。
- **粗查（多人）**：检测到对局后自动查询每位参战玩家的 ELO、K/D、胜率、样本数、偏好兵种、最爱单位（开战后自动查询，优先查敌人那队）。每名玩家 1 次请求，机器人/观战/重复自动跳过。玩家卡片带 📋 复制（一行情报），每队标题带「复制单队」。
- **龙区分**：在同分段、同角色构成的玩家里排第几，换算成 1~10（5.5 = 中位数；前 10% 真龙、前 30% 有龙样、后 30% 有点区、后 10% 纯区），同时显示「可能范围」。拉最近 20 场排位局（1 小时缓存），每场看分值 K/D、摧毁贡献（都和同 ELO、同角色构成、同分差的玩家比；占点和地图、选位强相关，不计入）和胜负（按双方赛前 ELO 的预期修正，队友掉线按以少打多算），用卡尔曼滤波汇总。角色按生涯兵种花费和最常用单位分成装甲/步兵/侦察/炮兵/防空/直升机/固定翼。附原因、分项、逐场表（每场标龙/区/泯）。所有参数用 BATrace 真实数据拟合；怎么算见 [docs/algorithm.md](docs/algorithm.md)，设计与回测见 [docs/rating-design.md](docs/rating-design.md)。
- **龙区复盘 + 称号（单局）**：「🕘 上一局」和对局档案详情里，名字前的大字是**龙 / 区 / 泯**（这一局在同分段、同角色的玩家里前 20% / 后 20% / 其余；角色按这一局实际出的单位判断）；名字后是一个称号，只看这一局对战局的实际作用、不看 ELO：大腿（赢方净交换最高，摧毁 − 损失）、背锅侠（输方最低）、掉线狗（游戏逃兵标记或挂机，输了锅就是他的）、带不动、躺赢狗、孤勇者，以及收割机、保活能手、举重冠军、开送、刮痧师傅、落地成盒、蹲逼、地产大亨、舔舐者、奶妈、曼巴out、大炮兵主义等突出表现（每种只在少数对局出现，悬停看原因）。一个人符合多个称号时只显示最罕见的那个。BATrace 刚打完的局可能还没收录，会提示稍后再看。
- **单局复盘页**：「🕘 上一局」、对局档案详情里点「📊 详细复盘」，或者右键对局档案行 / 录像行。和龙区复盘共用同一次请求（24 小时缓存）。四个标签页：
  - **总览**：双方阵营（按出的单位判断俄 / 美）、平均 ELO、赛前预期胜率、ELO 变化、掉线的人；摧毁 / 损失 / 击杀 / 阵亡 / 伤害 / 出兵花费 / 补给 / 占点 / 单位存活率的双方对比；兵种构成；全场之最；自动生成的「本局要点」（以弱胜强、交换比悬殊、飞机损失占比高、最赚和最亏的单位、损失最惨的一分钟等）。
  - **玩家**：每个人的龙区分、称号、ELO 变化、净交换、摧毁 / 损失、K/D、击杀 / 阵亡、伤害 / 承伤、出兵数和花费、单位存活率、阵亡单位的存活时间、每点花费打出的伤害、补给、占点，可按任意一列排序；点一行展开这个人的兵种构成、三个分项的百分位、补给往来、误伤、拆建筑，以及他出的每一种单位（出兵、阵亡、死亡率、存活时间、伤害、击杀、花费）。
  - **单位**：全场每一种单位的使用率（花费占本队的比例）、出兵数、均价、死亡率、阵亡单位存活时间、伤害、击杀、伤害 / 花费、谁在用，可按队伍筛选、按任意一列排序。
  - **时间线**：双方场上兵力曲线（累计出兵 − 累计损失）和每分钟损失，标出掉线时间和损失最惨的一分钟。
  - 单位库只有基础价格（不含配装），所以花费按每个人的官方出兵分、损失分等比例折算：队伍总数和官方一致，单个单位的花费是估算。
- **玩家粗查报告**：ELO、K/D、DMR、胜率、偏好兵种、最爱单位、地图表现、打法风格、近期对局表，支持复制单行情报。
- **玩家搜索**：按名字或 ID 搜索 BATrace 玩家。
- **对局档案**：自动保存最近 500 局，表格显示 状态（胜/负/观战/未知）｜模式（排位/自定义）｜地图｜ELO 变化 / 结算 ELO｜时间｜账号；右键可「🔄 刷新对局信息」。点开详情：对局信息 + 胜方/败方/观战三组玩家表格（龙区 / 名字 / ID / 队伍 / ELO 变化 / 评分 / 占点 / 击杀 / 伤害 / 承伤 / K-D / 补给 / 经验 / 奖牌），本地缺数据时自动从 API 补齐。
- **卡组工具**：前线卡组 / 后勤仓库两列。备份 / 恢复游戏卡组（前线 Decks/*.dek ⇄ 后勤 DeckBackups/*.zip），支持「备份全部」「备份选中」、一键部署、删除、打开目录。每局开始自动把当前前线卡组打包成唯一的「上一局卡组包.zip」，检测到换号时提醒是否替换。
- **玩家追踪**：持续记录见过的玩家（ID / 名字 / 改名史 / 相遇对局），存本地 `players-db.json`，以对局 ID 为唯一键。
- **右键「调查」**：玩家卡片、搜索候选、报告与龙区分页头的玩家名，右键「🔍 调查羁绊」：最新 ELO、最近 10 局、相遇次数、队友/敌人次数与胜负、相遇记录、改名历史、是否被封、胜率/偏好/最爱情报（6 小时缓存，可刷新）。
- **我的对局同步（每小时）**：拉取 `/api/players/matches?stbid=<本机ID>&limit=10`，按对局 ID 回填地图名 / 时长 / 胜负 / ELO / 模式 / 评分。观战局（TeamId=100）状态恒为「观战」。
- **封禁追踪（每小时）**：拉取 `/api/leaderboard/ban?limit=500`，**只对「之前遇到过」的新被封玩家弹提醒**；「🛡 封禁追踪」栏目默认隐藏，设置里勾选才显示。
- **上一局**：「🕘 上一局」查看最近一场已完成对局的名单并自动粗查、自动龙区复盘；再点按钮或有新对局时回到当前视图。
- **主题**：黑 / 白 / 青 / 橙 / 紫夜 / 深林 / 深海 七种，设置里选择即预览。
- **行车记录仪（对局录像）**：自动录制开关在卡片右上角（关掉只停止自动录制，已有录像照常可播）。每局开始自动录制游戏所在屏幕，结束后保存为 **MP4**，只存本地：
  - **画面**：FFmpeg 桌面复制（DXGI）采集 + 显卡硬件编码（NVIDIA NVENC / AMD AMF / Intel QuickSync，都没有时用 x264），CPU 占用约 0.3%～4%。分辨率可选 原生 / 1440p / 1080p / 720p，帧数 30～60，码率 3～40Mbps（默认 1080p / 30fps / 8Mbps，一小时约 3.6GB）。
  - **HDR**：Windows 开了 HDR 时自动改用 16 位浮点采集，以系统「SDR 内容亮度」为基准做色调映射（旧版 8 位采集会把 80 尼特以上全截成纯白，这就是以前过曝的原因）。显卡不支持 Vulkan 时自动退回普通采集。
  - **声音**：Windows WASAPI 回环采集默认播放设备（多声道自动混成立体声），与画面按采集时刻对齐（实测误差 < 60ms）。首次录制会用系统自带的 .NET 编译器生成采集小程序，缓存在 `%APPDATA%/broken-arrow-log-assistant/bin`。
  - **容错**：录制中桌面复制失效（切分辨率 / 全屏切换 / UAC 弹窗）自动续录新的一段，结束时合并；分片 MP4 格式，程序崩溃也不丢已录部分。
  - **播放**：卡片按时间倒序列出录像；对局档案里有录像的对局标 📹，点击播放。进度条可随意拖动，支持 1x/2x/3x；右键录像可用系统播放器打开 / 打开位置 / 打开对局详情 / 删除。旧版 WebM 录像第一次播放时会无损补上进度条索引。
  - **曝光与预览**：「录像设置」里的「曝光补偿」（±2 EV，每 +1 亮度翻倍）修正录像偏暗/偏亮；「📷 录制效果预览」可在开着游戏时截一张看效果，拖曝光滑块实时重新渲染。
  - **管理**：「录像设置」可选录制屏幕、分辨率、帧数、码率、曝光、声音、保存目录（换目录时可一键迁移）；「设置 → 本地录像管理」可删除 30 天前 / 全部。录制日志在 `%APPDATA%/broken-arrow-log-assistant/replay.log`。
- **BATrace 稳定性灯（顶栏 ●）**：每小时用几个轻量接口探测一次：全通=绿、部分=黄、全挂=红、未检测=灰；悬停看明细。
- **新版本提醒**：启动 8 秒后、之后每 6 小时查一次本仓库 GitHub Release 的最新版本号，有新版就在页面顶部提示，点「打开下载页」下载。不会自动下载或安装：安装版运行新的 Setup 覆盖安装，免安装版换成新的 exe，设置和档案都保留。
- **离线兜底**：本地保存「上次已知 ELO/胜率/偏好」快照；API 不可用时显示本地快照，搜索回退为「本地见过的玩家」。

> 请求间隔固定 1.2 秒/次（约 1 次/秒，适配 BATrace 限流），不在设置界面暴露。

## 环境要求

- Windows 10/11 + [Node.js](https://nodejs.org/)（建议 18+）
- 录像：Windows 自带的 .NET Framework 4（编译声音采集小程序，Win10/11 默认就有）；HDR 色调映射需要显卡支持 Vulkan
- 游戏《断箭》（Broken Arrow），日志目录形如 `C:\Program Files (x86)\Steam\steamapps\common\broken_arrow\GameLogs`

## 安装与运行

```powershell
npm install            # 首次：安装依赖
npm run fetch-ffmpeg   # 首次：下载录像用的 FFmpeg 到 vendor/ffmpeg/（约 180MB，带 SHA256 校验，不入库）
npm start
```

启动后在「设置」里选日志目录：点「自动检测 Steam 目录」，或点「浏览…」手动选。没装游戏时，把任意 `Gamelog__*.log` 复制到一个文件夹、目录指过去也能测试。

数据（设置、缓存、对局档案、玩家库、录像）都在 `%APPDATA%/broken-arrow-log-assistant/`，改名前后是同一个目录。

## 打包为 EXE

```powershell
npm install
npm run fetch-ffmpeg   # 打包时会把 ffmpeg.exe 放进 resources/ffmpeg/
npm run dist           # 安装版 + 便携版，输出到 dist/
```

- 产物：`龙区分类器 Setup x.y.z.exe`（安装版）与 `龙区分类器 x.y.z.exe`（便携版，双击即用）。
- 图标取自根目录 `logo.png`（打包时由 `scripts/prepare-icon.js` 生成 `build/icon.png`）。
- 版本号改 `package.json` 的 `version`（显示在标题栏和设置底部）。
- 推 `v*` 标签时 `.github/workflows/build.yml` 会在 Windows 上自动下载 FFmpeg、打包并创建 GitHub Release；也可在 Actions 页面手动运行只出产物。

## 技术要点

- **CORS 与 Electron**：BATrace 接口不带 `Access-Control-Allow-Origin`，所有请求都走主进程（Node 环境，不受 CORS 限制），界面只通过 IPC 拿数据。
- **日志读取**：只增量读取最新日志文件的新增字节；游戏重启生成新日志时自动切换文件。
- **核心接口**：
  - `GET /api/analysis/player?stbid=` ELO 趋势 / 胜负 / 最爱单位 / 偏好兵种 / 地图表现 / 打法——粗查与报告都用它，6 小时缓存
  - `GET /api/players/matches?stbid=&limit=20` 最近对局（每场含全部玩家），龙区分用，1 小时缓存；一次最多 20 场，`offset` 翻页
  - `GET /api/match?matchid=` 单局原始数据（含每个单位的记录、逃兵标记），龙区复盘和称号用，24 小时缓存
  - `GET /api/players/search?q=&limit=` 玩家搜索（10 分钟缓存）
  - 接口是 protobuf 转 JSON，值为 0 的字段会被省略（`TeamId` / `WinnerTeam` 缺失 = 0）
- **录像时间轴**：每帧时间戳改成「采集时刻 − T0」（`setpts=RTCTIME`），编码时间基 1/90000；进编码器前另分一路记录第一帧的采集时刻，合成时以它为零点对齐声音。
- **录像 HDR**：HDR 状态与「SDR 内容亮度」直接用 Windows DisplayConfig API 查询（`src/displayColor.js`）；FP16 帧在进 libplacebo 之前不能经过 swscale（会把浮点截到 [0,1]）。
- **龙区分与称号**：算法在 `src/dragonScore.js`、`src/matchTitles.js`（纯函数），参数在 `src/dragonModel.json`，由 `scripts/fit-dragon-model.js` 用 `backtest-data/` 里的真实数据拟合（采集：`scripts/pick-dragon-sample.js` + `scripts/collect-dragon-data.js`）。

## 已知限制

- 老对局（2026-08 之前的部分对局）在 BATrace 中缺少部分字段，胜负显示为「未知」。
- 最近 20 场里排位局太少时龙区分的可能范围会很宽、分数偏向中间。
- BATrace 没有击杀对象记录，判断不了「一个人打两个」这类局部情况，只能按整队缺人修正；也没有投降投票记录，不知道是谁发起的投降。
- 玩家的龙区分没法按阵营（俄/美）分开统计：「最近对局」接口里没有单位数据。
- 只读日志 + 公开接口，不碰内存、不注入、不影响反作弊。

## 目录结构

```
main.js                 Electron 主进程（窗口 / IPC / 录像保存与 replay:// 播放协议）
preload.js              渲染进程安全桥接
src/config.js           设置读写 + Steam 目录自动检测
src/logParser.js        日志解析状态机（对局 / 房间名单 / 卡组）
src/logWatcher.js       目录轮询 + 增量读取
src/batrace.js          BATrace API 客户端（限流 + 磁盘缓存 + 人机验证兜底）
src/batraceGate.js      BATrace 人机验证窗口
src/apiHealth.js        BATrace 稳定性灯（每小时轻量探测）
src/analyzer.js         玩家分析（粗查报告 / 龙区分与单局复盘取数）
src/dragonScore.js      龙区分算法（角色判断、逐场标准化打分、卡尔曼汇总、单局龙/区/泯）
src/matchTitles.js      单局称号（大腿 / 背锅侠 / 掉线狗 / 各种突出表现，不看 ELO）
src/matchReport.js      单局复盘页的数据（双方对比、玩家明细、单位使用率/死亡率、兵力时间线、本局要点）
src/dragonModel.json    龙区分与称号的全部参数（真实数据拟合生成）
src/tracker.js          玩家追踪库（相遇、改名、封禁）
src/storage.js          本地对局档案
src/deckSync.js         卡组同步（上一局卡组包、换号提醒）
src/zip.js              卡组打包 / 解包
src/ffmpegRecorder.js   对局录像：显示器/编码器探测、FFmpeg 采集编码、HDR 色调映射、分段续录、音画合成
src/audioLoopback.js    系统声音采集（编译并启动 WASAPI 回环小程序，实时编码 AAC）
src/displayColor.js     查询显示器 HDR 状态与 SDR 内容亮度（Windows DisplayConfig API）
src/replayLocal.js      本地录像列表 / 删除 / 清理
src/s3Client.js         录像文件名编解码（对局 ID / 上传者 / 队伍 / 地图）
native/wasapi-loopback.cs  WASAPI 回环采集小程序源码（运行时用系统 csc 编译）
scripts/fetch-ffmpeg.js        下载并校验 FFmpeg（开发与 CI 共用）
scripts/pick-dragon-sample.js  按 ELO 分层挑建模样本（不发请求）
scripts/collect-dragon-data.js 采集建模数据（缓存在 backtest-data/，不入库）
scripts/fit-dragon-model.js    拟合模型、回测、校准称号阈值，生成 src/dragonModel.json
docs/algorithm.md       龙区分与称号的算法说明（公式 + 完整例子）
docs/rating-design.md   龙区分与称号的设计取舍、数据、拟合与回测结果
renderer/               界面（原生 HTML/CSS/JS；单局复盘页在 renderer/matchReport.js）
.github/workflows/      GitHub Actions 自动打包（推 v* 标签即发布，打包前自动下载 FFmpeg）
```
